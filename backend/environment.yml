name: teleprompt-aligner
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pyaudio
  - pip:
      - fastapi
      - uvicorn[standard]
      - pydantic
      - pytest
      - httpx
      - websockets